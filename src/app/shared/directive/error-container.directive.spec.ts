import { ErrorContainerDirective } from './error-container.directive';

describe('ErrorContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorContainerDirective();
    expect(directive).toBeTruthy();
  });
});
